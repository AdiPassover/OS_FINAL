#ifndef OS_FINAL_PIPELINE_HANDLER_HPP
#define OS_FINAL_PIPELINE_HANDLER_HPP

#include <functional>
#include <queue>
#include <thread>
#include <mutex>
#include <condition_variable>

class PipelineHandler {
//public:
//    using Stage = std::function<void(int&)>;
//
//    PipelineHandler();
//    ~PipelineHandler();
//
//    PipelineHandler& add_stage(const Stage& stage);
//    PipelineHandler& add_stages(const std::initializer_list<Stage>& stages);
//
//    void run_pipeline(int& data);
//    void stop();
//
//private:
//    std::queue<int> _tasks;
//    std::vector<Stage> _stages;
//    std::vector<std::thread> _threads;
//    std::mutex _mutex;
//    std::condition_variable _cv;
//    bool _shutdown;

};


#endif //OS_FINAL_PIPELINE_HANDLER_HPP
